{% extends '_template.html.twig' %}

{% block title %}Modifier la Programmation{% endblock %}

{% block content %}
    <h1>Modifier la programmation</h1>

    {{ include('shoot/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shoot_index') }}">back to list</a>

    {{ include('shoot/_delete_form.html.twig') }}
{% endblock %}
