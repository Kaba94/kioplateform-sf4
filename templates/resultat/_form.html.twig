{{ form_start(form) }}
    {{ form_row(form.shoot) }}
    {{ form_row(form.volumeEnvoye) }}
    {{ form_row(form.resultat) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
